version: "3"

services:
  app:
    build:
        context: .
        dockerfile: Dockerfile
    image: dsradozhitskiy/devops-example-app

  caddy:
    build:
      context: .
      dockerfile: services/caddy/Dockerfile
    image: dsradozhitskiy/devops-example-caddy

